services:
  - type: web
    name: claimflow
    runtime: node
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
    healthCheckPath: /api/auth/login
