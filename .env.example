# Government Expense Tracker - Environment Configuration
# Copy this file to .env and update values for your environment

# ═══════════════════════════════════════════════════════════════════
# APPLICATION SETTINGS
# ═══════════════════════════════════════════════════════════════════

# Application Environment (development, staging, production)
NODE_ENV=production

# Server Port (default: 3000)
PORT=3000

# Application Name & Version
APP_NAME="Government Expense Tracker"
APP_VERSION="1.0.0"

# Base URL for the application (used in emails, redirects)
BASE_URL=https://your-app.railway.app

# ═══════════════════════════════════════════════════════════════════
# DATABASE CONFIGURATION
# ═══════════════════════════════════════════════════════════════════

# SQLite Database Path (for local development)
DATABASE_PATH=./database/expenses.db

# PostgreSQL Database (for production - Railway provides this automatically)
# DATABASE_URL=postgresql://username:password@host:port/database_name

# Database Connection Pool Settings
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_IDLE_TIMEOUT=30000

# ═══════════════════════════════════════════════════════════════════
# SECURITY SETTINGS
# ═══════════════════════════════════════════════════════════════════

# Session Secret (CRITICAL: Change this to a random string in production)
SESSION_SECRET=your-super-secure-random-session-secret-change-this-in-production

# Password Salt (CRITICAL: Change this in production)
PASSWORD_SALT=your-secure-password-salt-change-this

# JWT Secret (if using JWT tokens)
JWT_SECRET=your-jwt-secret-key

# Session Timeout (in milliseconds - default: 8 hours)
SESSION_TIMEOUT=28800000

# Maximum Login Attempts
MAX_LOGIN_ATTEMPTS=5

# Account Lockout Duration (in milliseconds - default: 15 minutes)
LOCKOUT_DURATION=900000

# ═══════════════════════════════════════════════════════════════════
# FILE UPLOAD SETTINGS
# ═══════════════════════════════════════════════════════════════════

# Upload Directory Path
UPLOAD_PATH=./uploads

# Maximum File Size (in bytes - default: 10MB)
MAX_FILE_SIZE=10485760

# Allowed File Types (comma-separated)
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp,application/pdf

# File Storage Service (local, aws-s3, azure-blob, google-cloud)
FILE_STORAGE=local

# ═══════════════════════════════════════════════════════════════════
# CLOUD STORAGE (if using cloud storage)
# ═══════════════════════════════════════════════════════════════════

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# Azure Blob Storage Configuration
AZURE_STORAGE_ACCOUNT=your-azure-account
AZURE_STORAGE_KEY=your-azure-key
AZURE_CONTAINER=your-container-name

# Google Cloud Storage Configuration
GOOGLE_CLOUD_PROJECT_ID=your-project-id
GOOGLE_CLOUD_KEYFILE=path/to/keyfile.json
GOOGLE_CLOUD_BUCKET=your-bucket-name

# ═══════════════════════════════════════════════════════════════════
# EMAIL CONFIGURATION (for notifications)
# ═══════════════════════════════════════════════════════════════════

# Email Service Provider (smtp, sendgrid, mailgun, ses)
EMAIL_PROVIDER=smtp

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-email-password

# SendGrid Configuration
SENDGRID_API_KEY=your-sendgrid-api-key

# Mailgun Configuration
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain

# Email Templates
FROM_EMAIL=noreply@yourcompany.gov
FROM_NAME="Government Expense System"

# ═══════════════════════════════════════════════════════════════════
# LOGGING & MONITORING
# ═══════════════════════════════════════════════════════════════════

# Log Level (error, warn, info, debug)
LOG_LEVEL=info

# Log File Path
LOG_PATH=./logs/app.log

# Enable Audit Logging
AUDIT_LOGGING=true

# Audit Log Path
AUDIT_LOG_PATH=./logs/audit.log

# External Monitoring (optional)
MONITORING_URL=https://your-monitoring-service.com/webhook

# ═══════════════════════════════════════════════════════════════════
# GOVERNMENT COMPLIANCE SETTINGS
# ═══════════════════════════════════════════════════════════════════

# Government Department/Agency Name
DEPARTMENT_NAME="Your Government Department"

# NJC Rate Version (for compliance tracking)
NJC_RATE_VERSION=2026.1

# Fiscal Year
FISCAL_YEAR=2026

# Enable Strict Compliance Mode
STRICT_COMPLIANCE=true

# Require Digital Signatures
REQUIRE_DIGITAL_SIGNATURE=false

# Data Retention Period (in days)
DATA_RETENTION_DAYS=2557

# ═══════════════════════════════════════════════════════════════════
# INTEGRATION SETTINGS
# ═══════════════════════════════════════════════════════════════════

# External Systems
ERP_SYSTEM_URL=https://your-erp-system.com/api
ERP_API_KEY=your-erp-api-key

# LDAP/Active Directory Integration
LDAP_ENABLED=false
LDAP_URL=ldap://your-domain-controller.com
LDAP_BIND_DN=cn=service-account,ou=users,dc=yourcompany,dc=gov
LDAP_BIND_PASSWORD=service-account-password
LDAP_BASE_DN=ou=users,dc=yourcompany,dc=gov

# Single Sign-On (SAML)
SAML_ENABLED=false
SAML_ENTRY_POINT=https://your-idp.com/sso/saml
SAML_ISSUER=your-app-identifier
SAML_CERT=path/to/saml-cert.pem

# ═══════════════════════════════════════════════════════════════════
# PERFORMANCE & SCALING
# ═══════════════════════════════════════════════════════════════════

# Enable Caching
REDIS_ENABLED=false
REDIS_URL=redis://localhost:6379

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=900000

# Compression
ENABLE_GZIP=true

# ═══════════════════════════════════════════════════════════════════
# DEVELOPMENT & DEBUGGING (disable in production)
# ═══════════════════════════════════════════════════════════════════

# Enable Debug Mode
DEBUG=false

# Enable Detailed Error Messages
DETAILED_ERRORS=false

# Enable CORS for development
CORS_ENABLED=false
CORS_ORIGIN=http://localhost:3000

# Disable SSL in development only
DISABLE_SSL=false